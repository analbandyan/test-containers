spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: mq_mysql
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true